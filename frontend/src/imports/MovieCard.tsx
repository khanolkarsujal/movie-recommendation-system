/**
 * MovieCard.tsx
 * Named alias for EpisodeCard.tsx — matches the folder structure spec.
 * `import MovieCard from './MovieCard'` resolves to EpisodeCard's implementation.
 * All hover animation, progress bar, and thumbnail logic stay in EpisodeCard.tsx.
 */
export { default } from './EpisodeCard';
