/**
 * HeroSection.jsx
 * Named alias for Hero.jsx — allows the canonical import path
 * `import HeroSection from './HeroSection'` while keeping all
 * animation/shader logic centralized in Hero.jsx.
 */
export { default } from './Hero';
