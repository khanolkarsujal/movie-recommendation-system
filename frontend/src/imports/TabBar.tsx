/**
 * TabBar.tsx
 * Named alias for SeasonTabBar.tsx — provides the canonical
 * `import TabBar from './TabBar'` path per the folder structure spec.
 * All sliding-indicator logic and CSS stay in SeasonTabBar.tsx.
 */
export { default } from './SeasonTabBar';
