/**
 * MovieCard.jsx
 * Named alias for EpisodeCard.jsx — matches the folder structure spec.
 * `import MovieCard from './MovieCard'` resolves to EpisodeCard's implementation.
 * All hover animation, progress bar, and thumbnail logic stay in EpisodeCard.jsx.
 */
export { default } from './EpisodeCard';
